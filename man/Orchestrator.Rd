% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_orchestrator.R
\name{Orchestrator}
\alias{Orchestrator}
\title{Orchestrator R6 Class}
\description{
Base orchestration class that provides generalized nested-object access and
synchronization hooks.
}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{metadata}}{List containing orchestration metadata.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-Orchestrator-initialize}{\code{Orchestrator$new()}}
    \item \href{#method-Orchestrator-pre_sync_state}{\code{Orchestrator$pre_sync_state()}}
    \item \href{#method-Orchestrator-post_sync_state}{\code{Orchestrator$post_sync_state()}}
    \item \href{#method-Orchestrator-sync_state}{\code{Orchestrator$sync_state()}}
    \item \href{#method-Orchestrator-access_nested}{\code{Orchestrator$access_nested()}}
    \item \href{#method-Orchestrator-set_nested}{\code{Orchestrator$set_nested()}}
    \item \href{#method-Orchestrator-set}{\code{Orchestrator$set()}}
    \item \href{#method-Orchestrator-clone}{\code{Orchestrator$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Orchestrator-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-Orchestrator-initialize}{}}}
\subsection{\code{Orchestrator$new()}}{
  Creates a new Orchestrator object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Orchestrator$new()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new Orchestrator object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Orchestrator-pre_sync_state"></a>}}
\if{latex}{\out{\hypertarget{method-Orchestrator-pre_sync_state}{}}}
\subsection{\code{Orchestrator$pre_sync_state()}}{
  Hook executed before \code{sync_state()} logic.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Orchestrator$pre_sync_state(
  field = NULL,
  member = NULL,
  target_field = NULL,
  name = NULL,
  role = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{field}}{Optional top-level field name.}
      \item{\code{member}}{Optional nested member name.}
      \item{\code{target_field}}{Optional destination field path.}
      \item{\code{name}}{Optional named list entry inside \code{field}.}
      \item{\code{role}}{Optional role-based list resolution key.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Invisibly returns \code{NULL}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Orchestrator-post_sync_state"></a>}}
\if{latex}{\out{\hypertarget{method-Orchestrator-post_sync_state}{}}}
\subsection{\code{Orchestrator$post_sync_state()}}{
  Hook executed after \code{sync_state()} logic.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Orchestrator$post_sync_state(
  field = NULL,
  member = NULL,
  target_field = NULL,
  name = NULL,
  role = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{field}}{Optional top-level field name.}
      \item{\code{member}}{Optional nested member name.}
      \item{\code{target_field}}{Optional destination field path.}
      \item{\code{name}}{Optional named list entry inside \code{field}.}
      \item{\code{role}}{Optional role-based list resolution key.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Invisibly returns \code{NULL}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Orchestrator-sync_state"></a>}}
\if{latex}{\out{\hypertarget{method-Orchestrator-sync_state}{}}}
\subsection{\code{Orchestrator$sync_state()}}{
  Synchronize orchestrator state.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Orchestrator$sync_state(
  field = NULL,
  member = NULL,
  target_field = NULL,
  name = NULL,
  role = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{field}}{Optional top-level field name.}
      \item{\code{member}}{Optional nested member name.}
      \item{\code{target_field}}{Optional destination field path.}
      \item{\code{name}}{Optional named list entry inside \code{field}.}
      \item{\code{role}}{Optional role-based list resolution key.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Invisibly returns \code{self}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Orchestrator-access_nested"></a>}}
\if{latex}{\out{\hypertarget{method-Orchestrator-access_nested}{}}}
\subsection{\code{Orchestrator$access_nested()}}{
  Generalized accessor for nested objects stored on the class.

Resolves a top-level field (for example \code{tools} or \code{framework}),
optionally resolves a named element if the field is a list, and then
returns a nested field or invokes a nested method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Orchestrator$access_nested(
  field,
  name = NULL,
  member = NULL,
  role = NULL,
  update_sync = FALSE,
  update_modified = TRUE,
  ...
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{field}}{Character scalar naming the stored top-level field.}
      \item{\code{name}}{Optional character scalar naming a list element in
\code{field}.}
      \item{\code{member}}{Optional character scalar naming a field or method on the
resolved object.}
      \item{\code{role}}{Optional character scalar used to resolve a list element in
\code{field} by role-like name (for example \code{"household"} for
\code{"tool_household_iphra_v2"}).}
      \item{\code{update_sync}}{Logical indicating whether to synchronize state after access.}
      \item{\code{update_modified}}{Logical indicating whether to update the modified timestamp.}
      \item{\code{...}}{Arguments passed to the nested method when \code{member}
resolves to a function.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The requested nested value, or the nested method result.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Orchestrator-set_nested"></a>}}
\if{latex}{\out{\hypertarget{method-Orchestrator-set_nested}{}}}
\subsection{\code{Orchestrator$set_nested()}}{
  Generalized nested field setter.

Resolves a top-level field (optionally a named list element), sets
\code{member} to \code{value}, then calls synchronization hooks and
updates the modified timestamp.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Orchestrator$set_nested(field, member, value, name = NULL, role = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{field}}{Character scalar naming the stored top-level field.}
      \item{\code{member}}{Character scalar naming a writable field in the resolved
nested object.}
      \item{\code{value}}{Value to assign.}
      \item{\code{name}}{Optional character scalar naming a list element in
\code{field}.}
      \item{\code{role}}{Optional character scalar used to resolve a list element in
\code{field} by role-like name.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Invisibly returns \code{self}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Orchestrator-set"></a>}}
\if{latex}{\out{\hypertarget{method-Orchestrator-set}{}}}
\subsection{\code{Orchestrator$set()}}{
  Generalized, scope-safe field setter.

Mirrors \code{access_nested()}'s \code{field}/\code{name}/\code{role}/
\code{member} arguments, but safely writes \code{value} instead of
reading. Unlike \code{set_nested()}, \code{field} may resolve to either
a public or a private field, and \code{member} is optional: when
omitted, \code{value} replaces the resolved top-level (or
name/role-resolved) target directly. Writing to a resolved member or
target that currently holds a function is rejected, to avoid
accidentally clobbering methods.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Orchestrator$set(
  field,
  value,
  member = NULL,
  name = NULL,
  role = NULL,
  update_sync = FALSE,
  update_modified = TRUE
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{field}}{Character scalar naming a public or private top-level
field on this object.}
      \item{\code{value}}{Value to assign.}
      \item{\code{member}}{Optional character scalar naming a writable field on the
resolved target. When \code{NULL}, \code{value} is assigned directly
to the resolved target.}
      \item{\code{name}}{Optional character scalar naming a list element in
\code{field}.}
      \item{\code{role}}{Optional character scalar used to resolve a list element in
\code{field} by role-like name.}
      \item{\code{update_sync}}{Logical indicating whether to synchronize state
after the assignment.}
      \item{\code{update_modified}}{Logical indicating whether to update the
modified timestamp.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Invisibly returns \code{self}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Orchestrator-clone"></a>}}
\if{latex}{\out{\hypertarget{method-Orchestrator-clone}{}}}
\subsection{\code{Orchestrator$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Orchestrator$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
