% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_sample_drawing.R
\name{draw_sample_psu_systematic_rlc}
\alias{draw_sample_psu_systematic_rlc}
\title{Draw PSUs using systematic-random-selection RLC sampling}
\usage{
draw_sample_psu_systematic_rlc(
  frame,
  sample_size,
  n_sites,
  cluster_size = 3,
  seed = 42
)
}
\arguments{
\item{frame}{Data frame. Eligible PSUs.  A \code{population_size} column
is optional; if absent or all-zero the cluster allocation is evenly
distributed across the selected sites.}

\item{sample_size}{Integer. Total household sample size (used to derive
\code{n_clusters = ceiling(sample_size / cluster_size)}).}

\item{n_sites}{Integer. Number of sites (PSUs) to select.  Required.}

\item{cluster_size}{Integer. Households per cluster (default \code{3}).}

\item{seed}{Integer. Random seed for reproducibility (default \code{42}).}
}
\value{
\code{frame} with \code{sampled_psu} and \code{allocated_sample}
  columns added.  PSUs outside the pre-selected sites receive \code{NA}.
}
\description{
First selects \code{n_sites} primary sampling units via systematic random
sampling.  Then allocates \code{ceiling(sample_size / cluster_size)} cluster
slots across those pre-selected sites \strong{proportional to population
size} when a \code{population_size} column is present and sums to a positive
value; otherwise slots are distributed evenly.  \code{n_sites} must be
provided.
}
\details{
Reserve clusters (RC) are drawn at the cluster-allocation stage: 3 RC when
\code{n_clusters <= 10}, 4 when \code{10 < n_clusters <= 20}, and 5 when
\code{n_clusters > 20}.
}
