% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_sample_drawing.R
\name{draw_sample_psu_proportional_rlc}
\alias{draw_sample_psu_proportional_rlc}
\title{Allocate cluster slots proportional to population size across all PSUs}
\usage{
draw_sample_psu_proportional_rlc(
  frame,
  sample_size,
  cluster_size = 3,
  seed = 42
)
}
\arguments{
\item{frame}{Data frame. Eligible PSUs with a \code{population_size} column.}

\item{sample_size}{Integer. Total household sample size (used to derive
\code{n_clusters = ceiling(sample_size / cluster_size)}).}

\item{cluster_size}{Integer. Households per cluster (default \code{3}).}

\item{seed}{Integer. Random seed for RC position draw (default \code{42}).}
}
\value{
\code{frame} with \code{sampled_psu} and \code{allocated_sample}
  columns added.  All PSUs are selected.  PSUs that receive only reserve
  cluster slots have \code{allocated_sample = NA}.
}
\description{
All eligible PSUs are included; \code{ceiling(sample_size / cluster_size)}
main cluster slots are allocated proportionally to each PSU's population
size (Hamilton largest-remainder method).  Reserve clusters (RC) are
interleaved with the main slots: 3 RC when \code{n_clusters <= 10}, 4 when
\code{10 < n_clusters <= 20}, and 5 when \code{n_clusters > 20}.
}
\details{
Requires a \code{population_size} column.  \code{n_sites} does not apply —
all eligible PSUs participate.
}
